services:
  - type: web
    name: lllit.bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python reservas_main.py"
    plan: free
    envVars:
      - key: PORT
        value: 5000