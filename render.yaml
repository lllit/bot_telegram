services:
  - type: web
    name: telegram-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python reservas_main.py"
    plan: free
    envVars:
      - key: PORT
        value: 5000